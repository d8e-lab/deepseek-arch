# DeepSeek Arch v2.0.0

> 两个大能力同时到位：它现在会**记住你**（长期记忆），子代理也从"一次性任务"变成**可以继续对话的助手**。

## 🧠 长期记忆：它开始记住你的偏好了

以前每次开新会话都要重新交代一遍习惯（"回复先给结论""测试别打桩"）。v2.0 起，这些偏好会被自动记住，
并在**新会话开始时自动生效**——你不需要说"记住这个"，也不需要确认。

### 它怎么工作

1. **对话结束后后台归纳**：一个独立的记忆代理读最近的对话，判断里面有没有"跨会话仍然有意义"的内容
   （偏好、约定、边界），有就写下来。它与你对话**并发运行**，不阻塞、不拖慢回复，失败也不打扰你。
2. **新会话自动注入**：写下的事实以清单形式注入系统提示，模型一开始就知道你的偏好。
3. **需要细节时按需读取**：清单只给一行摘要，模型觉得相关时会去读全文。

记忆分两层：**项目层**（跟仓库走：`{工作区}/.deepseek-arch/memory/`）与**全局层**
（跨项目的个人偏好：`~/.deepseek-arch/memory/`）。

### 生命周期：用进废退，但不会偷偷删

每条记忆有一个"置信度"，档位本身就是它的生命周期：

| 档位 | 含义 | 模型能看到吗 |
|:--|:--|:--|
| 3 / 2 | 正式记忆（你明确说过 / 确认过） | ✅ 会注入 |
| 1 | 待观察（模糊、拿不准的） | ❌ |
| 0 | 待销毁（记忆装不下了，最久没用的先下线） | ❌ |

- **按"活动日"老化**：只有"你真的用了程序"的那天才算一天。**半年没开程序，回来不会被清空**。
- **闲置不致死**：长期没人提最多降到"待观察"，不会因为"没人用"就被删；
  只有记忆**总量超过上限**（默认 400 条）时，最久没用的才会进入待销毁，再闲置一段才归档。
- **归档而非删除**：默认移到 `legacy/archive/` 保留文件，可手动找回。
- **被再次用到就会回来**：重新启用从"待观察"开始，继续被用才升回正式记忆。

### 你能做什么

| 想做的事 | 命令 |
|:--|:--|
| 看状态（条数、开关、参数） | `/memory` |
| 列出记忆 / 关键词过滤 | `/memory show [关键词]` |
| 看"待观察 / 待销毁"的条目 | `/memory candidates` |
| 跑一次清理并**预览**（不落盘） | `/memory gc --dry-run` |
| **永久保留**某条（免疫自动清理） | `/memory pin <slug>` |
| 立即遗忘某条 | `/memory forget <slug>` |
| 开关 / 立即刷新注入 | `/memory on`、`/memory off`、`/memory refresh` |
| 完全关闭长期记忆（启动时） | `--no-memory` |

写入**不需要确认**；所有自动动作（升降级、换出、归档）都会记审计日志。
`[memory]` 配置段有 21 个可调项（阈值、注入预算、模型、开关），详见 `docs/config.md`。

## 🤖 子代理可以继续对话了

子代理不再"跑完即终结"：

- **续跑**：已完成、失败、甚至被取消的子代理，都能用 `subagent_send`（或 Ctrl+T 视图内直接输入）
  追加指令继续干活——完整对话上下文都保留着。
- **查轨迹**：新增 `subagent_trace` 工具，能看到子代理执行过哪些工具、传了什么参数（不含思维链）。
- **崩溃可恢复**：子代理记录增量落盘，进程中断后重新恢复会话仍能继续。
- **通知更克制**：结果被取走后不再重复提示，异步模式下也只在真正有结果时出现一行提示。

## 🖥️ 顺手改好的工程细节

- **工作区 runtime 目录统一**：规划文档、记忆、API 请求镜像、文件状态都收在 `{工作区}/.deepseek-arch/`
  （不参与版本控制），不再散落。
- **`--workspace <dir>`**：一条命令指定工作区根目录（决定 runtime 与项目层记忆的落点）。
- **`chat --prompt "<内容>"`**：非交互单轮执行，只把回复打到 stdout——方便写脚本、接定时任务
  （也是后续"心跳/主动唤起"的载体）。

## 🔧 修复

- **`--no-memory` 之前其实没生效**：它只关闭了注入，工具仍然注册、runtime 文件照建。
  现在关闭得更彻底：不注入、不归纳、从工具集里剔除记忆工具，也不创建任何记忆目录。
- **Windows 上 shell 命令输出的中文乱码**：统一 UTF-8 与流式解码。
- **无工具调用的轮次会丢失注入的提醒块**：修复后子代理通知与记忆提醒不再"下一次请求就消失"。

## ⚠️ 升级注意（本次为破坏性版本，故升 major）

- **移除了 YOLO 审查模型（reviewer / censor agent）与自动续答**：相关配置键 `defaults.review_model`
  与命令 `/review_model` 已删除；如果你的 `config.toml` 里还有它们，删掉即可（不删也不报错）。
- **子代理记录文件布局变更**：由单文件 `<name>.json` 改为目录 `<name>/meta.json` + 分代轮次文件，
  旧格式不再读取（历史子代理记录不会自动迁移）。
- **记忆是全新功能**，旧版本没有相关配置：`[memory]` 段可按需增补，不配置也会用默认值工作。

## 📦 安装

```bash
# npm（推荐，2.0.0 已发布）
npm install -g deepseek-arch@2.0.0

# 从源码
git clone git@github.com:d8e-lab/deepseek-arch.git && cd deepseek-arch
npm install && npm run build && node dist/cli/index.js
```

> AUR（`yay -S deepseek-arch`）本次**未同步更新**，仍停留在旧版本；需要 2.0.0 请用 npm 或源码安装。

## 🔜 后续计划

已经排上、但这一版还没做的（详细清单见仓库 `docs/todo/`）：

- **心跳 / 主动唤起**：让它在你不主动开口时也能提醒"到期事项"（你曾说过的"下周再看"之类）；
  载体已备好（`chat --prompt`），还差 cron/systemd 细节与无人值守时的呈现方式。
- **记忆命令面补充**：按层级过滤、审计查看、单独开关后台归纳代理等便利项。
- **时间兜底**：目前"归档"只在记忆总量超限时发生——是否需要一条纯时间的兜底（如两年未被用到即归档）待定。
- 每次归纳的原始日志落盘、记忆更新提示事件化（供外部订阅）。

---

**升级建议**：直接 `npm i -g deepseek-arch@2.0.0` 即可。首次使用长期记忆无需任何配置；
若想先看看它会记什么，跑一次 `/memory show`，或 `/memory gc --dry-run` 预览自动清理会动哪些条目。
