# DeepSeek Arch v1.4.0 — Skill 机制重构（测试版）

> ⚠️ **测试版声明**：本版本包含全新 skill 机制，属于功能预览。核心能力已验证，欢迎反馈问题与建议后再正式发布。

> 一句话摘要：模型现在能"看到"并主动调用技能（规划、发布、调研、文档维护），不再需要你手动指定。

## 🧠 第一优先级：用户体验改善

### Skill 机制重构 — 模型能发现并调用技能

之前 deepseek-arch 的技能需要**硬编码绑定**（只有"规划"技能可通过专门指令触发），其他技能模型根本不知道存在。

**v1.4.0 起**，技能变成了一套完整的可发现机制：

| 能力 | 说明 |
|------|------|
| 🗂 **技能目录** | 对话开始时，模型自动获知可用技能及适用场景 |
| 🛠 **统一调用** | 一个"skill"工具即可调用任意技能，加新技能不用改代码 |
| 📄 **自定义简单** | 技能 = 一个 Markdown 文件 + 顶部元信息，放入 `skill/` 目录即生效 |

### 内置技能（4 个，覆盖三种模式）

- 🧠 **plan** — 编码任务规划与自检框架（含新增的"设计蓝图"环节）
- 🚀 **release** — 版本发布全流程
- 🔍 **research** — 独立技术调研（子代理隔离执行，不占用主对话）
- 📚 **docs** — 文档维护规范（自动出现在你编辑 docs/ 目录时）

### 规划框架增强

- **设计蓝图先行**：复杂任务先输出完整方案（数据流、接口、取舍）再拆解任务，你确认后再动手
- **规模判据分场景**：功能小改 vs 架构重构采用不同标准，避免过度设计误伤重构

## 🤖 第二优先级：新功能

- 🛠 **skill 工具**：对话中模型可主动调用技能（如"用 plan 规划"、"用 research 调研"）
- 🔀 **子代理执行**：技能可声明在隔离子代理中运行，主对话保持干净
- 🎯 **条件激活**：技能可绑定文件路径（如 docs/**），触碰相关文件时才出现
- 🔒 **动态确认**：技能可声明需要用户确认后再执行

## 🔧 Bug 修复

- 修复：技能列表超长时可能撑爆系统提示（现在有预算控制，自动截断）
- 修复：旧版会话恢复后技能记录兼容（plan 调用记录平滑迁移）

---

## 📦 安装

```bash
# Arch Linux (AUR)
yay -S deepseek-arch

# npm 全局安装
npm install -g deepseek-arch

# 从源码
git clone https://github.com/d8e-lab/deepseek-arch.git
cd deepseek-arch
npm install && npm run build
```

> 💡 **测试版反馈**：试用 `skill` 相关功能时遇到任何问题，请提交 issue 到
> https://github.com/d8e-lab/deepseek-arch/issues，标注 [skill-beta]。
