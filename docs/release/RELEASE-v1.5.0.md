# DeepSeek Arch v1.5.0

> 全双工 TUI + Subagent 会话化——让对话、子代理、命令输出真正"并行"起来。

## 🖥️ 全双工交互：输出期间也能操作

- **模型输出时 `/` 命令立即执行**——`/context`、`/yolo`、`/model` 等无需等输出结束
- **普通输入排队**——输出期间打字不中断，输出完自动发送
- **命令结果区固定底部**——命令输出与对话隔离、完整显示、不随对话滚动，像输入框下方的补全列表一样稳定

## 🤖 Subagent 会话化：能"追剧"的子代理

- **`subagent_send` 工具**——模型可向已完成的子代理追加指令续跑，上下文完整保留
- **Ctrl+T Subagents 总览视图**——实时查看子代理运行状态与输出，**vim 式交互**：按 `i` 进入输入模式（`n`/`p` 不再误吞输入字符），Enter 发送，ESC 返回导航
- **恢复会话**——重启后子代理执行记录从磁盘恢复，可继续交互

## 🛠 配置系统升级

- **`deepseek-arch init`**——一键初始化/迁移配置：生成完整模板、自动补全缺失项（`--force` 备份重新生成）
- **`context_window` 支持单位写法**——`"1M"`、`"256K"`，不用再数零
- **配置模板逐项注释**——每个键都说明作用与可选项，`temperature`/`max_tokens` 等参数即改即用

## 🔧 Bug 修复

- 修复了键入字符时输入框逐次上移、对话内容被清掉的问题
- 修复了流式期间键盘失效（命令、Ctrl+C、视图都无响应）的问题
- 修复了命令结果区超宽行导致渲染错位、输入区残留累积的问题
- 修复了子代理输出内容每几个词就换行、markdown 表格断裂的问题
- 修复了视图关闭时可能双流并发、输出交错的问题
- 修复了命令执行后输入区残留命令文本的问题

## 📦 安装

```bash
# Arch Linux (AUR)
yay -S deepseek-arch

# npm
npm install -g deepseek-arch

# 源码
npm run build && node dist/cli/index.js chat
```
