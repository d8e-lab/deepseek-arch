# DeepSeek Arch v1.5.3

> 内部架构全面组件化——界面稳定性和可维护性大幅提升，为后续新 UI 能力铺路。本次无功能变化，升级即用。

## 🧱 前端组件化重构（方案 A）

v1.5.2 之后，我们对 TUI 前端做了一次彻底的"拆积木"式重构：把原来集中在单个大模块里的界面逻辑，拆成**可独立拼装、独立测试的组件**。

### 界面拆分结果

| 组件 | 对应界面 |
|---|---|
| ConversationViewer | Ctrl+O 全屏对话浏览（滚动/搜索/轮次跳转） |
| SubagentsViewer | Ctrl+T Subagents 总览（轨迹滚动/视图内输入/实时刷新） |
| CommandResultPane | 底部 `/` 命令结果区 |
| SuggestionPane | 底部命令补全建议列表 |

### 共享渲染抽象

重构中发现了多处"各写一份"的重复实现，已统一收口为共享渲染组件，界面一致性更有保障：

- **列表渲染**（补全建议、交互选择器的滚动窗口 / 高亮 / 折叠提示统一实现）
- **滚动状态机**（滚动 / 翻页 / 贴底跟随统一实现）
- **按键序列解析**、**Markdown 段落渲染**（表格 + 折行 + 缩进）

### 收益

- **更稳**：组件各自独立测试（新增 57 个组件级测试，总计 465 个全绿）；界面行为由端到端逐帧测试兜底，与 v1.5.2 视觉完全一致
- **更快迭代**：改 Subagents 视图的滚动逻辑不再需要读懂整个主应用；任何组件可单独拿出来复用
- **为未来铺路**：有了统一组件契约，后续新 UI 能力（方案 B）可在此基础上搭建

## 🔧 本次无用户可见 Bug 修复

纯内部重构，不改变任何界面外观与交互——升级后看到的界面与 v1.5.2 完全一样，但底层更健康。

## 📦 安装

```bash
# Arch Linux (AUR)
yay -S deepseek-arch

# npm
npm install -g deepseek-arch
```

> 已安装旧版本的用户：`npm install -g deepseek-arch@latest` 升级即可。
